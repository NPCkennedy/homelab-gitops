apiVersion: v1
kind: Service
metadata:
  name: graphiti-mcp
  namespace: spencer-os
  labels:
    app: graphiti-mcp
    app.kubernetes.io/part-of: spencer-os
spec:
  selector:
    app: graphiti-mcp
  ports:
    - name: mcp
      port: 8000
      targetPort: 8000
